@using EntityLayer.Concrete
@model List<ContactMessage>

<div class="dropdown-menu dropdown-menu-right navbar-dropdown" aria-labelledby="notificationDropdown">
    <p class="mb-0 font-weight-normal float-left dropdown-header">Bildirimler</p>
    @foreach (var item in  Model)
    {
    <a class="dropdown-item" href="/Member/ContactMessage/MessageDetails/@item.MessageID">
        <div class="item-thumbnail">
            <div class="item-icon bg-success">
                <i class="mdi mdi-information mx-0"></i>
            </div>
        </div>
        <div class="item-content">
                <h6 class="font-weight-normal">Kimden: @item.ContactName</h6>
            <p class="font-weight-light small-text mb-0 text-muted">
               Konu: @item.ContactSubject
            </p>
        </div>
    </a>
    }

